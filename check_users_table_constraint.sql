-- Check users table constraints and structure
-- Run this in your Supabase SQL Editor to verify the table structure

-- 1. Check table structure
s
-- 5. Test query to see existing users (optional - remove phone numbers for privacy)
-- SELECT id, name, phone_number, created_at FROM users LIMIT 10;

